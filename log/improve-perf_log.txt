Branch: improve-perf
Date: 2026-06-28
Author: Saad Bhatti

========================================
SUMMARY OF CHANGES
========================================

Goal: The quad-tree neighbour search was slower than the brute-force O(n^2)
approach despite being theoretically O(n log n). Several bugs and inefficiencies
were identified and fixed across three files.

----------------------------------------
FILE: shapes.py
----------------------------------------

1. Circle.containsPt — Remove sqrt from distance check
   Before:
       dist = math.sqrt((self.x - point.x)**2 + (self.y - point.y)**2)
       return dist <= self.radius
   After:
       dx = self.x - point.x
       dy = self.y - point.y
       return dx * dx + dy * dy <= self.radius * self.radius

   Why: math.sqrt is expensive. Since we only need to compare the distance
   against a threshold, we can compare squared distances instead and avoid
   the sqrt entirely. This function is called for every point in every leaf
   node that overlaps the query circle — across all boids per frame, that
   amounts to O(n^2) sqrt calls eliminated.

2. Rectangle.intersects(Circle) — Replace 4x distToLine with AABB closest-point test
   Before:
       if self.containsPt(cx, cy): return True
       if (Point.distToLine(...) <= cr or   # left edge
           Point.distToLine(...) <= cr or   # right edge
           Point.distToLine(...) <= cr or   # top edge
           Point.distToLine(...) <= cr):    # bottom edge
           return True
       return False
   After:
       closest_x = max(l0, min(cx, r0))
       closest_y = max(t0, min(cy, b0))
       dx = cx - closest_x
       dy = cy - closest_y
       return dx * dx + dy * dy <= cr * cr

   Why: The original code ran up to 4 calls to distToLine, each computing a
   sqrt. The correct and cheaper approach is to find the closest point on the
   rectangle to the circle centre by clamping, then do a single squared-distance
   check. No sqrt needed. This also correctly handles the case where the circle
   centre is inside the rectangle (closest point IS the centre, distance = 0).
   This intersection test is called O(n log n) times per frame during tree
   traversal, so reducing it from 4 sqrts to 0 is a meaningful saving.

----------------------------------------
FILE: quad_tree.py
----------------------------------------

3. Node.__init__ — Fix mutable default argument (correctness bug)
   Before:
       def __init__(self, rectBoundary, points=[]):
           self.points = points
   After:
       def __init__(self, rectBoundary, points=None):
           self.points = points if points is not None else []

   Why: In Python, a mutable default argument (points=[]) is created once at
   class definition time and shared across all calls that don't supply the
   argument. Every Node() constructed without an explicit points list was
   referencing the same list object. Mutating it (via .append) on one node
   would corrupt all others. The fix uses None as the sentinel and creates a
   fresh list per instance.

4. recursiveInsert — Only subdivide when capacity is actually exceeded
   Before:
       if (not node.isSubdivided) and (len(node.points) <= capacity):
           node.points.append(point)
           recursiveSubdivide(node, capacity)   # called on EVERY insert
           return True
   After:
       node.points.append(point)
       if len(node.points) > capacity:          # only called when needed
           recursiveSubdivide(node, capacity, currHeight)
       return True

   Why: The original code called recursiveSubdivide after every single append,
   even when the node had plenty of room. recursiveSubdivide would return
   immediately in those cases (wasted call), but the overhead adds up. Now
   subdivision only happens when the capacity is actually exceeded.

   Additionally, the children loop was changed to return True immediately
   on the first successful child insert, rather than trying all siblings.

5. recursiveSubdivide — Add depth limit to prevent infinite recursion
   Before:
       def recursiveSubdivide(node, capacity):
           if len(node.points) <= capacity:
               return
           ...
           recursiveSubdivide(topLeft, capacity)   # no depth bound
   After:
       def recursiveSubdivide(node, capacity, currHeight):
           if len(node.points) <= capacity or currHeight == 0:
               return
           ...
           recursiveSubdivide(topLeft, capacity, currHeight - 1)

   Why: Without a depth bound, coincident points (boids at the exact same
   position, which happens naturally in this simulation) always fall into the
   same child quadrant. The child always exceeds capacity, which triggers
   another subdivision, and so on forever — producing a RecursionError. The
   currHeight parameter threads the tree's maxheight limit down into
   recursiveSubdivide so recursion is capped regardless of point distribution.

6. recursiveQuery — Remove redundant double intersection check
   Before:
       elif node.isSubdivided:
           for child in node.children:
               if child.rectBoundary.intersects(range):  # checked here
                   recursiveQuery(child, dataList, range)  # and again at top of call
   After:
       if node.isSubdivided:
           for child in node.children:
               recursiveQuery(child, dataList, range)  # check happens once inside

   Why: recursiveQuery already starts by checking if the node's boundary
   intersects the range and returning early if not. Checking it again in the
   parent's loop before the recursive call was redundant — each intersection
   test was being done twice. Removing the guard in the loop halves the number
   of intersection tests at internal nodes.

7. iterativeQuery — Replace queue.LifoQueue with a plain list stack
   Before:
       stack = queue.LifoQueue()
       stack.put(root)
       while not stack.empty():
           node = stack.get()
           ...
           stack.put(child)
   After:
       stack = [root]
       while stack:
           node = stack.pop()
           ...
           stack.extend(node.children)

   Why: queue.LifoQueue is a thread-safe data structure that uses locks on
   every put/get. This simulation is single-threaded — the locking overhead
   is pure waste. A plain Python list used as a stack (.append/.pop) is
   significantly faster for this use case. Also removed the import of the
   queue module since it's no longer used.

----------------------------------------
FILE: flock.py
----------------------------------------

8. inNeighboorhood — Exclude self from quad-tree results
   Before:
       neighbors.extend(self.quadtree.query(circleRange))
   After:
       neighbors.extend(b for b in self.quadtree.query(circleRange) if b is not myBoid)

   Why: The brute-force path already excluded the querying boid itself with
   `boid is not myBoid`. The quad-tree path did not — a boid was always within
   its own neighbour radius, so it always included itself in the results. This
   caused incorrect flocking behaviour when using the quad-tree.

9. draw — Remove redundant Boid type filter
   Before:
       boidNeighbors = [a for a in neighbors if type(a) == Boid]
       # ... then use boidNeighbors in cohesion/separation/alignment
   After:
       # neighbors used directly — no filter

   Why: The neighbors list already contains only Boid objects (inserted as
   such via insertPts). The filter was an O(n) pass per boid (O(n^2) total)
   that never removed anything.

10. draw — Remove second per-frame quad-tree rebuild
    Before:
        if showQtree:
            self.quadtree.reset()
            self.quadtree.insertPts(self.flock)  # second full rebuild
            self.quadtree.drawBoundaries()
    After:
        if showQtree:
            self.quadtree.drawBoundaries()  # reuse the tree built at frame start

    Why: The tree was being rebuilt twice every frame when showQtree was True:
    once at the top of draw() for querying, and again after all boids updated,
    just to draw the boundaries. The second rebuild costs a full O(n log n)
    insert pass for purely visual output. The tree built at the start of the
    frame is still valid for drawing — the boundaries shown will be one frame
    behind the boid positions, which is imperceptible at normal frame rates.

========================================
VERIFICATION
========================================

Two automated checks were run after the fixes:

1. Coincident points test: 50 boids inserted at the exact same (x, y) position.
   Both recursive and iterative query paths returned all 50 without a
   RecursionError.

2. Correctness test: 2000 random points inserted into the quad-tree.
   200 random circle queries were run and compared against a brute-force scan.
   Result: 0 mismatches out of 200 queries.


========================================
SECOND PASS - ADDITIONAL IMPROVEMENTS
========================================

A follow-up scan of the whole repository found further bugs, performance
wins, and code-quality issues. All of the following were applied.

----------------------------------------
FILE: quad_tree.py
----------------------------------------

11. Quadtree.insertPts — Fix broken type check (correctness bug)
    Before:
        if not type(object) == type(Point):
            if type(object) == tuple:
                ... Point(object[0], object[1], object) ...
            else:
                ... Point(object.x, object.y, object) ...
        else:
            ... insert object directly ...
    After:
        if isinstance(object, Point):
            ... insert object directly ...
        elif type(object) == tuple:
            ... Point(object[0], object[1], object) ...
        else:
            ... Point(object.x, object.y, object) ...

    Why: type(Point) evaluates to `type` (the metaclass), not the Point
    class. So `not type(object) == type(Point)` was `type(object) != type`,
    which is True for every normal object — the branch that inserts an
    already-built Point directly was dead code. It happened to work because
    only boids and tuples were ever passed. Replaced with isinstance, which
    correctly detects Point instances and preserves their .data.

----------------------------------------
FILE: flock.py
----------------------------------------

12. Add explicit `import math`
    Why: avoidMouse uses math.copysign, but flock.py never imported math.
    It was only available by accident, leaking through
    `from quad_tree import *` -> `from shapes import *` -> `import math`.
    If shapes ever stopped importing math, flock would break. Added a direct
    import so the dependency is explicit and robust.

13. Fuse cohesion / seperation / alignment into a single pass (applyRules)
    Before: three separate methods, each looping over the full neighbor list,
    called from draw() as:
        if useCohesion:   acc += .004 * self.cohesion(boid, neighbors)
        if useSeperation: acc += 1.1  * self.seperation(boid, neighbors)
        if useAlignment:  acc += .5   * self.alignment(boid, neighbors)
    After: one method, applyRules(boid, neighbors, useCohesion, useSeperation,
    useAlignment), that accumulates the cohesion position-sum, alignment
    velocity-sum, and separation vector in a single loop, then combines them
    with the same weights.

    Why: the neighbor list was traversed up to three times per boid per frame.
    Fusing traverses it once. The combined acceleration is numerically
    identical to the original (verified: max difference 0.0).

14. Remove redundant distance filter in separation (behavioral, negligible)
    Before:
        if boid.pos.distance_to(neighbor.pos) < boid.neighborRadius:
            ... contribute to separation ...
    After:
        ... every neighbor contributes (no filter) ...

    Why: every neighbor returned by the query is already within
    neighborRadius, so the filter was redundant. It also computed a sqrt via
    distance_to and then as_polar() recomputed the same magnitude immediately
    after — duplicate work, O(boids x neighbors) per frame. The only
    behavioral change is at the exact-radius boundary (< vs <=), which is
    imperceptible.

----------------------------------------
FILE: boid.py
----------------------------------------

15. limitSpeed — Replace polar conversion with length()/scale_to_length
    Before:
        r, phi = self.velocity.as_polar()
        if r > self.maxSpeed:
            self.velocity.from_polar((self.maxSpeed, phi))
    After:
        if self.velocity.length() > self.maxSpeed:
            self.velocity.scale_to_length(self.maxSpeed)

    Why: numerically equivalent magnitude clamp without the trig of
    as_polar/from_polar. Called once per boid per frame.

16. update — Update the boid's Circle in place instead of reallocating
    Before:
        self.circle = Circle(self.x, self.y, self.neighborRadius)
    After:
        self.circle.x = self.x
        self.circle.y = self.y

    Why: a fresh Circle was allocated every boid every frame; only x and y
    change (radius is constant), so updating in place avoids the churn.

----------------------------------------
FILES: main.py, test.py
----------------------------------------

17. Guard module-level main() with `if __name__ == "__main__":`
    Why: both files called main() at import time, so merely importing either
    module (e.g. for testing) would immediately launch the pygame loop. The
    guard makes them importable without side effects.

========================================
VERIFICATION (SECOND PASS)
========================================

1. Numeric equivalence: per-boid acceleration from the fused applyRules
   compared against the original separate-rule math (filter removed) across
   a 250-boid flock. Max difference: 0.0.

2. Smoke test: 30 frames of the real draw() loop ran headlessly (SDL dummy
   driver) with no exceptions; all rule-toggle combinations ran without error.

3. insertPts dispatch: verified tuple, Point, and object-with-x/y inputs all
   insert correctly, and a Point's .data is preserved (previously-dead branch
   now reachable).


========================================
THIRD PASS - GPU-READY VECTORIZED BACKEND (Phase 0 + 1)
========================================

Branch: improve-more-perf
Date: 2026-06-28
Commit: a1f1864

Goal: lay the groundwork for offloading the flocking computation to the GPU
(via CuPy / CUDA). Rather than jump straight to CuPy, the work was structured
so the valuable refactor lands on CPU first and the GPU becomes a drop-in swap.

Assessment that shaped the approach:
  - At the current scale (~350 boids) the bottleneck is Python per-object
    overhead, not raw math. A GPU would actually be SLOWER here because of
    kernel-launch + host<->device transfer overhead; the crossover where a GPU
    wins is in the thousands-to-tens-of-thousands of boids.
  - The object + quad-tree design is the opposite of what a GPU wants. GPUs
    want Structure-of-Arrays (contiguous pos/vel/... arrays) and a parallel
    brute-force N*N neighbour search; a quad-tree is serial pointer-chasing
    that does not vectorize. The GPU path therefore discards the quad-tree.
  - CuPy mirrors the NumPy API, so writing the physics against a module handle
    `xp` lets the SAME code run on NumPy (CPU) now and CuPy (GPU) later.
  - Environment finding: CuPy 14.1.1 imports but cannot reach the device --
    `cudaErrorInsufficientDriver`. The NVIDIA driver (~451.67) is too old for
    CuPy 14's CUDA 12 runtime. The GPU is a modest older mobile chip (paired
    with Intel HD 520). Phase 2 (GPU) is blocked until the driver is updated or
    a CUDA-11-matched CuPy (cupy-cuda11x) is installed.

----------------------------------------
NEW FILE: flock_array.py
----------------------------------------

18. Vectorized Structure-of-Arrays flock
    The whole flock lives in arrays (pos, vel, radius, radius2, max_speed,
    size, color). One step() advances every boid at once using array math over
    an N*N pairwise-distance matrix:
      - neighbour[i,j] = dist2 <= radius_i^2 and i != j (per-row radius, so the
        original asymmetric "my radius" semantics are preserved).
      - cohesion:   0.004 * (mean(neighbour positions) - own position)
      - alignment:  0.5   * (mean(neighbour velocities) - own velocity)
      - separation: 1.1   * sum_j (p_i - p_j) / dist2  (coincident pairs add 0)
      - boids with no neighbours get zero rule-acceleration; a boid whose total
        acceleration is exactly zero gets the same small random "wander" nudge
        as the original.
    Then integrate: v += a; clamp |v| to max_speed; p += v; wrap at edges.
    All rule weights and the per-boid radius/max_speed/size distributions mirror
    boid.py / flock.py so behaviour matches the object path.

    Written against an `xp` module handle (numpy now, cupy later) at float32.
    Includes add_boid / remove_boid for interactive parity and an optional
    pygame draw() (a per-boid Python loop -- deliberately the Amdahl ceiling,
    not the thing being optimized).

    Intentional semantic difference: the update is Jacobi (all boids read the
    previous frame's state) vs the object path's in-place Gauss-Seidel (later
    boids see earlier boids' already-updated positions). Jacobi is
    order-independent and the natural fit for vectorized / GPU execution; the
    emergent flocking is equivalent.

----------------------------------------
NEW FILE: benchmark.py
----------------------------------------

19. Headless benchmark harness
    Runs the simulation with rendering disabled (SDL dummy driver, fixed seed)
    and reports ms/frame for the object/quad-tree flock vs the array flock
    across a range of N, plus the speedup. A --gpu flag swaps the array backend
    to CuPy and falls back to NumPy with a warning if the GPU is unavailable.
    The object path is isolated to physics by monkeypatching Boid.draw to a
    no-op for the duration of the measurement.

----------------------------------------
FILE: main.py
----------------------------------------

20. Runtime toggle for the vectorized backend
    Press 6 to switch between the object/quad-tree backend (default) and the
    array backend at runtime. The array flock is created lazily on first toggle,
    seeded with the current boid count. Mouse-add, Backspace-remove, and the
    3/4/5 rule toggles all route to whichever backend is active; the caption and
    console line show the live backend.

21. Fix separation toggle (correctness bug)
    The `4` key toggled a misspelled `useSeperation` variable that was separate
    from the `useSeparation` the simulation actually reads, so toggling
    separation was a no-op (and risked a NameError). Corrected to `useSeparation`
    throughout.

----------------------------------------
FILE: boid.py
----------------------------------------

22. maxSpeed tuning: gauss(12,3) -> gauss(20,5)
    Pre-existing tuning on this branch; the array backend mirrors gauss(20,5)
    so the two backends stay consistent.

----------------------------------------
FILE: requirements.txt
----------------------------------------

23. Added numpy (now a hard dependency); noted optional cupy-cuda1x for the
    GPU backend. Rewritten from UTF-16 to clean UTF-8.

========================================
RESULTS / VERIFICATION (THIRD PASS)
========================================

CPU benchmark (headless, physics only, NumPy backend, 40 frames):

       N     quadtree ms        array ms     speedup
     350          28.222           5.638       5.01x
    1000          93.995          39.416       2.38x
    2000         227.170         141.467       1.61x
    3000         379.897         353.866       1.07x

Interpretation: the array backend wins decisively at the current operating size
(5x at N=350) where Python object overhead dominates. The speedup decays with N
because the dense O(N^2) matrix crosses over with the quad-tree's O(N log N)
near N~3000 on CPU. That crossover is exactly the case for the GPU (parallelize
the O(N^2)) and motivates a future spatial-hash grid (Phase 3) for either
backend.

Correctness (300 steps, 350 boids): positions/velocities all finite, positions
stay in-bounds, 0 speed-clamp violations, mean ~5 neighbours (flocking is
genuinely happening). Integration path (step -> draw -> add_boid/remove_boid)
verified headlessly under the SDL dummy driver.

Status: Phase 0 (benchmark) + Phase 1 (vectorized CPU backend) complete.
Phase 2 (CuPy swap) is ready in code but blocked on the NVIDIA driver / CuPy
CUDA-runtime mismatch described above.


========================================
FOURTH PASS - BACKEND STATE SYNC, GPU ENABLEMENT (Phase 2), SEPARATION TUNING
========================================

Branch: attempt-cuda
Date: 2026-06-28
Commits: 8cc7a82 (backend sync), aeb42a7 (GPU + separation)

----------------------------------------
A. SHARED FLOCK STATE BETWEEN BACKENDS  (commit 8cc7a82)
----------------------------------------

Problem: toggling between the object/quad-tree backend and the array backend
(key 6) showed two independent simulations with different random boids. Now the
toggle carries the SAME flock across in both directions.

24. flock_array.py: FlockArray.from_boids(boids, w, h, xp) classmethod
    Builds the array flock from existing Boid objects, copying every per-boid
    attribute (pos, vel, size, neighborRadius, maxSpeed, color) exactly. Used
    when entering array mode so it continues the current flock rather than a
    fresh random one. Verified bit-exact on entry.

25. flock_array.py: write_to_flock(flock)
    Rebuilds flock.flock from the array state when leaving array mode, so the
    object backend resumes the same boids. Handles boids added/removed while in
    array mode (counts stay in sync). step() also early-returns when n == 0 so
    the flock can be emptied without error.

26. main.py: key 6 now syncs state across the toggle (from_boids on entry,
    write_to_flock on exit) instead of creating an independent random flock.

----------------------------------------
B. GPU / CUDA ENABLEMENT  (Phase 2, commit aeb42a7)
----------------------------------------

The Phase 2 blocker (driver/runtime mismatch) was resolved. The driver had been
updated to 522.06 (CUDA 11.8). The remaining problem was the installed CuPy:
cupy-cuda13x bundles a CUDA 13 runtime that a CUDA-11.8 driver cannot run, and
CUDA 13 dropped support for this GPU's architecture entirely. Fix:

  - Swapped cupy-cuda13x -> cupy-cuda11x (CuPy 13.6), matching the driver.
  - cupy-cuda11x does not bundle the CUDA libraries and there is no system CUDA
    Toolkit, so installed the nvidia-*-cu11 library wheels (nvrtc, cuda-runtime,
    cublas, curand, cusparse, cusolver, cufft).
  - Those wheels scatter DLLs across site-packages/nvidia/<lib>/bin, which
    cupy-cuda11x does not auto-register on Windows.

GPU: NVIDIA GeForce (Maxwell, compute capability 5.0, ~1 GB VRAM).

27. NEW FILE cuda_setup.py
    On import (Windows only), registers each site-packages/nvidia/<lib>/bin dir
    with os.add_dll_directory so CuPy can resolve nvrtc64_112_0.dll etc. No-op on
    non-Windows or when the nvidia wheels aren't installed. Import BEFORE cupy.

28. flock_array.py: host-side random init
    The one-time random init (_init_state, add_boid, the per-frame wander jitter,
    from_boids) now uses a NumPy host RNG and moves results to the device via
    xp.asarray. This sidesteps numpy.random vs cupy.random API differences (CuPy
    Generator lacks .normal) and makes the initial flock identical on CPU and GPU.

29. main.py: --gpu and --boids N flags
    --gpu selects the CuPy backend for the array flock (resolve_array_backend),
    falling back to NumPy with a message if the GPU is unavailable. --boids sets
    the initial flock size. The caption/console label shows the active backend.
    Run: python main.py --gpu --boids 3000, then press 6.

30. benchmark.py: import cuda_setup before cupy; catch GPU OutOfMemoryError and
    report "OOM" instead of crashing; free the device memory pool between sizes.

GPU benchmark (headless, physics only):
       N     quadtree ms        array(GPU) ms     speedup
     350         114              24               4.8x
    1000         261              15              17x
    2000         349              29              12x
    4000         848              90               9.4x
    6000        1406             OOM                -
    8000        1993             OOM                -

CPU<->GPU parity: identical init (diff 0.0); after 5 steps max position diff
~1.2e-4 (float32 reduction-order), GPU output finite. The ~1 GB VRAM caps the
dense N*N approach near N~5000 -- the main motivation for a Phase 3 GPU
spatial-hash grid.

----------------------------------------
C. SEPARATION WEIGHT SCALES WITH FLOCK SIZE  (commit aeb42a7)
----------------------------------------

31. flock.py / flock_array.py: separation weight is now SEPARATION_K / N (total
    flock size) instead of a fixed 1.1. SEPARATION_K = 385 is calibrated so the
    weight equals the previous 1.1 at the default 350-boid flock; it weakens as
    the flock grows (e.g. 0.385 at N=1000, 0.128 at N=3000) and strengthens as it
    shrinks (7.7 at N=50). The same constant is defined in both backends so they
    stay identical. "Number of boids" = total flock size, not neighbour count.

Status: Phase 2 (GPU backend) complete and verified. Remaining: Phase 3 (GPU
spatial-hash grid) to get past the ~1 GB VRAM / O(N^2) ceiling.


========================================
FIFTH PASS - SPATIAL-HASH GRID NEIGHBOUR SEARCH (Phase 3)
========================================

Branch: spatial-hashing
Date: 2026-06-28

Goal: replace the brute-force O(N^2) distance matrix with a memory-light spatial
grid so the flock can scale past the dense matrix's ~1 GB VRAM ceiling (which
OOM'd around N~5000 on this GPU).

32. flock_array.py: step() refactored into a shared rule-combiner + two pluggable
    neighbour accumulators, selected by a new use_grid flag (constructor and
    from_boids). Both return the same (count, sum_pos, sum_vel, sep), so the rule
    math downstream is identical regardless of how neighbours were found.
      - _accumulate_bruteforce(): the existing dense N x N path, unchanged.
      - _accumulate_grid(): the new spatial-hash grid (below).

33. flock_array.py: _accumulate_grid()
    Uniform grid with cell size = R_max (the largest neighbour radius). Because
    every boid's radius <= R_max, all its neighbours fall in its own cell or the
    8 around it (a 3x3 block). Boids are counting-sorted into a dense
    (n_cells, max_occupancy) table of indices (argsort by cell, bincount/cumsum
    for offsets, scatter into the table). For each of the 9 cell offsets the whole
    candidate cell is gathered at once as an (N, max_occupancy) array and reduced
    -- the slot dimension is fully vectorised, so the per-frame kernel count stays
    ~9 instead of 9 * max_occupancy (an earlier slot-loop version was launch-bound
    at ~2400 ms/frame; vectorising the slots fixed it). Out-of-range offsets are
    masked, not clamped, so edge cells are never double-counted; self is excluded
    via j != i.

34. benchmark.py: --grid flag (threads use_grid into the array flock); header
    shows the neighbour-search method.

35. main.py: --grid flag and runtime key 7 to toggle grid vs brute-force on the
    array backend; the caption shows Array-<backend>-grid vs -brute.

CORRECTNESS: grid vs brute-force on identical init -> count diff exactly 0 (same
neighbour sets); sums differ only by float32 summation order (sum_pos ~4e-3).
Trajectories stay together over 60 steps (mean pos diff ~4e-5).

PERFORMANCE (headless, physics only):
  CPU (NumPy) grid vs quad-tree:  N=1000 8.6x, N=3000 4.0x.
  GPU (CuPy)  grid vs quad-tree:  N=2000 5.4x, N=5000 9.0x, N=8000 9.6x.
  GPU grid vs GPU brute-force:    brute wins below ~N=4000 (grid has fixed
    sort/table overhead); grid wins above, and runs at N=8000-15000 where brute
    OOMs. Grid roughly triples the max flock size on the 1 GB card (~5k -> ~15-20k).

CAVEATS:
  - Not asymptotically O(N): cell size is pinned to R_max for correctness, so on
    a fixed screen n_cells is fixed and cost is ~O(N^2 / n_cells) -- a large
    constant-factor win (and the big memory win), not linear. An outlier large
    radius inflates cell size and hurts efficiency.
  - Below ~N=1000 brute-force is faster; use grid for large N.
  - Windows GPU TDR watchdog: a single grid step taking >~2 s (roughly N>15k
    here) can reset the driver for that process. Keep steps under ~1 s.

Status: Phase 3 (spatial-hash grid) complete and verified. Possible future work:
sub-R_max cells with a per-boid variable search ring (true O(N) scaling), or
batched rendering to lift the per-boid draw-loop Amdahl ceiling at large N.
